% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.mcq_analysis.R
\name{plot.mcq_analysis}
\alias{plot.mcq_analysis}
\title{Plot a difficulty-discrimination scatter for an mcq_analysis object}
\usage{
\method{plot}{mcq_analysis}(
  x,
  y = NULL,
  discrimination_metric = c("point_biserial", "discrimination_index"),
  label = c("flagged", "all", "none"),
  flag_threshold_difficulty = c(0.3, 0.9),
  flag_threshold_discrimination = 0.3,
  point_cex = 1.4,
  label_cex = 0.75,
  ...
)
}
\arguments{
\item{x}{An object of class \code{mcq_analysis}.}

\item{y}{Ignored. Present for S3 compatibility.}

\item{discrimination_metric}{Which discrimination index to plot on the
y-axis. One of \code{"point_biserial"} (default) or
\code{"discrimination_index"}.}

\item{label}{One of \code{"flagged"} (default, label only problematic items),
\code{"all"} (label every item), or \code{"none"} (no labels). Also accepts
\code{TRUE} (= "all") or \code{FALSE} (= "none") for backwards compatibility.}

\item{flag_threshold_difficulty}{Numeric vector of length 2 giving the
informative difficulty range. Default \code{c(0.30, 0.90)}.}

\item{flag_threshold_discrimination}{Numeric. Discrimination cutoff
below which an item is considered weak. Default 0.30.}

\item{point_cex}{Numeric. Point size. Default 1.4.}

\item{label_cex}{Numeric. Label text size. Default 0.75.}

\item{...}{Additional graphical parameters passed to \code{plot()}.}
}
\value{
The input \code{mcq_analysis} object, invisibly.
}
\description{
Produces the classical item quality map: a scatterplot of item
difficulty (x-axis) against item discrimination (y-axis), with
reference lines marking conventional adequacy cutoffs. Items in the
upper-middle region (medium difficulty, high discrimination) are
performing well; items in the lower regions are candidates for
revision.
}
\details{
By default, only flagged items (those falling outside the conventional
adequacy region) are labeled, to keep the plot legible when many items
cluster in the acceptable region. Use \code{label = "all"} to label every
item, or \code{label = "none"} to suppress labels entirely.

Reference lines are drawn at conventional cutoffs from Ebel and
Frisbie (1991): discrimination >= 0.30 (acceptable) and difficulty
between 0.30 and 0.90 (informative range).
}
\examples{
data(mcq_example)
result <- mcq_analysis(mcq_example$responses, mcq_example$key)
plot(result)
plot(result, label = "all")
plot(result, label = "none")

}
\references{
Ebel, R. L., & Frisbie, D. A. (1991). \emph{Essentials of educational
measurement} (5th ed.). Prentice Hall.
}
